# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
# El patrón cubre cualquier directorio que empiece por `.next`, no solo los dos
# que conocíamos: una copia manual llamada `.next-stale-20260801` cayó fuera de
# `.next/` y `.next-playwright*/`, y se colaron 1 GB de caché de webpack al
# repositorio —con ficheros de 466 MB que GitHub rechaza al recibir.
.next*/
out/
build/

# Env
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Prisma migrations are source code and must be versioned.

# OS
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*

# Playwright (live fixtures may contain temporary credentials)
test-results-live/
playwright-report-live/
test-results/
playwright-report/
backups/

# DB backups, data exports and restore-drill evidence (may contain personal data)
exports/
*.dump
*.sql.gz

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Rastros de las herramientas de desarrollo: configuración local de editores y
# agentes, e instantáneas de depuración. Se quedan en disco, fuera del repo.
.claude/
.cursor/
.codex/
.cursorrules
AGENTS.md
CLAUDE.md
.playwright-cli/
